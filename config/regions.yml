---
# Target Regions Configuration
# Regions where AMIs will be copied after successful build
# Format: Comma-separated list of AWS region codes

# Default target regions for AMI distribution
target_regions:
  - us-east-2      # US East (N. Virginia)
  - ca-central-1       # Canada (Central)
  

